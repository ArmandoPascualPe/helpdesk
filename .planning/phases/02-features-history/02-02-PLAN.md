---
phase: 02-features-history
plan: 02
type: execute
wave: 1
depends_on: []  # Depends on Phase 1 completion
files_modified:
  - src/lib/search.ts
  - src/actions/search-tickets.ts
  - src/components/ticket-search-bar.tsx
  - src/components/ticket-filter-panel.tsx
  - src/components/ticket-pagination.tsx
  - src/app/tickets/page.tsx
autonomous: true
requirements: [SECH-01, SECH-02, SECH-03, SECH-04, SECH-05, SECH-06]
must_haves:
  truths:
    - "User can search tickets by text"
    - "User can filter by status/priority/category/date"
    - "Search results paginate correctly"
  artifacts:
    - path: "src/lib/search.ts"
      provides: "SearchFilters, SearchResult, buildPocketBaseFilter function"
    - path: "src/actions/search-tickets.ts"
      provides: "searchTickets Server Action with role-based filtering"
    - path: "src/components/ticket-search-bar.tsx"
      provides: "Text search input with debounce"
    - path: "src/components/ticket-filter-panel.tsx"
      provides: "Filter dropdowns for status/priority/category/date"
    - path: "src/components/ticket-pagination.tsx"
      provides: "Pagination controls (10 per page)"
  key_links:
    - from: "src/app/tickets/page.tsx"
      to: "src/actions/search-tickets.ts"
      via: "searchTickets action"
      pattern: "searchTickets.*action"
    - from: "src/components/ticket-search-bar.tsx"
      to: "src/app/tickets/page.tsx"
      via: "onSearch prop"
      pattern: "onSearch.*setSearch"
    - from: "src/components/ticket-filter-panel.tsx"
      to: "src/app/tickets/page.tsx"
      via: "onFilterChange prop"
      pattern: "onFilterChange.*setFilters"
---

<objective>
Implement Search & Filter system with pagination
</objective>

<context>
@.planning/phases/02-features-history/02-CONTEXT.md
@.planning/phases/02-features-history/02-RESEARCH.md
@src/lib/pocketbase.ts
@src/lib/auth.ts
@src/lib/validations.ts

**Locked Decisions to implement exactly:**
- D-10: Text search on title and description (LIKE %query%)
- D-11: Filters: status, priority, category, date_from, date_to
- D-12: Pagination: 10 results per page
- D-13: Combined queries (search + filters)
</context>

<execution_context>
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Search library and Server Action</name>
  <files>src/lib/search.ts, src/actions/search-tickets.ts</files>
  <read_first>
    - src/lib/validations.ts - for Zod patterns
    - src/lib/auth.ts - for currentUser() and role checking
  </read_first>
  <action>
    **Create `src/lib/search.ts`:**
    - Export interface SearchFilters:
      ```typescript
      interface SearchFilters {
        query?: string;           // Text search
        status?: string;          // Filter by status
        priority?: string;       // Filter by priority
        category?: string;        // Filter by category
        date_from?: string;       // ISO date string
        date_to?: string;        // ISO date string
        page?: number;            // Default 1
        per_page?: number;        // Default 10 (D-12)
      }
      ```
    - Export interface SearchResult with tickets[], totalItems, totalPages, currentPage
    - Export function buildPocketBaseFilter(filters: SearchFilters): string
      - Combines query with LIKE on title OR description (D-10)
      - Adds status filter (D-11)
      - Adds priority filter
      - Adds category filter
      - Adds date_from and date_to filters
      - Uses && to combine all filters (D-13)

    **Create `src/actions/search-tickets.ts`:**
    - Server Action `searchTickets(filters: SearchFilters)`
    - Validates using searchSchema from validations
    - Gets currentUser() for role-based filtering
    - For cliente role: filters by user_id = currentUser.id
    - For agente role: filters by departamento = currentUser.departamento
    - For supervisor: no filter (can see all)
    - Calls buildPocketBaseFilter
    - Returns SearchResult with pagination meta
  </action>
  <verify>
    <automated>grep -r "buildPocketBaseFilter" src/lib/search.ts && grep -r "searchTickets" src/actions/search-tickets.ts</automated>
  </verify>
  <done>
    - Search library with filter builder (D-10, D-11, D-13)
    - Server Action with role-based access control
    - Pagination support (D-12)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Search and Filter UI components</name>
  <files>src/components/ticket-search-bar.tsx, src/components/ticket-filter-panel.tsx, src/components/ticket-pagination.tsx</files>
  <read_first>
    - src/app/tickets/page.tsx - to understand tickets list page
  </read_first>
  <action>
    **Create `src/components/ticket-search-bar.tsx`:**
    - Props: onSearch(query: string)
    - Text input for query search
    - Button triggers search action
    - Debounced search (300ms) for UX

    **Create `src/components/ticket-filter-panel.tsx`:**
    - Props: onFilterChange(filters: SearchFilters)
    - Dropdown for status: ["nuevo", "en_proceso", "resuelto", "cerrado", "en_espera", "reabierto"] (D-11)
    - Dropdown for priority: ["baja", "media", "alta", "critica"] (D-11)
    - Dropdown for category: ["hardware", "software", "red", "otros"] (D-11)
    - Date pickers for date_from and date_to (D-11)
    - "Clear filters" button
    - All filters trigger onFilterChange on change

    **Create `src/components/ticket-pagination.tsx`:**
    - Props: currentPage, totalPages, onPageChange(page: number)
    - Shows "Page X of Y"
    - Previous/Next buttons
    - Page number links (1, 2, 3, ...)
    - Disabled state for first/last page
  </action>
  <verify>
    <automated>ls src/components/ticket-search-bar.tsx src/components/ticket-filter-panel.tsx src/components/ticket-pagination.tsx</automated>
  </verify>
  <done>
    - Search bar with debounced text search
    - Filter panel with status/priority/category/date filters
    - Pagination controls for 10 per page
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Search & Filter into tickets list page</name>
  <files>src/app/tickets/page.tsx</files>
  <read_first>
    - src/app/tickets/page.tsx - current state
    - src/components/ticket-search-bar.tsx
    - src/components/ticket-filter-panel.tsx
    - src/components/ticket-pagination.tsx
  </read_first>
  <action>
    **Update `src/app/tickets/page.tsx`:**
    - Add state for search filters (query, status, priority, category, date_from, date_to, page)
    - Add state for search results (tickets[], totalItems, totalPages)
    - Integrate ticket-search-bar component
    - Integrate ticket-filter-panel component
    - Integrate ticket-pagination component
    - On mount, fetch initial tickets (page 1)
    - On search/filter/page change, refetch tickets
    - Show loading state during fetch
    - Show "No tickets found" when empty
  </action>
  <verify>
    <automated>grep -r "SearchFilters" src/app/tickets/page.tsx && grep -r "searchTickets" src/app/tickets/page.tsx</automated>
  </verify>
  <done>
    - Tickets list page has search functionality (D-10)
    - Tickets list page has filter panel (D-11)
    - Results paginated at 10 per page (D-12)
    - Combined queries work (search + filters)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→searchTickets | Untrusted filter params cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | S | searchTickets | mitigate | Validate all filter inputs with Zod, sanitize query string |
| T-02-07 | I | search results | mitigate | Filter by user_id for cliente role, by department for agente |
</threat_model>


[Overall phase checks]
- Text search works on title and description
- All filters work (status, priority, category, date range)
- Pagination shows 10 results per page
- Combined queries work
</verification>

<success_criteria>
- SECH-01: User can search tickets by text in title/description (LIKE %query%)
- SECH-02: User can filter by status
- SECH-03: User can filter by priority
- SECH-04: User can filter by category
- SECH-05: User can filter by date range
- SECH-06: Results paginated (10 per page)
</success_criteria>

<output>
After completion, create `.planning/phases/02-features-history/02-02-SUMMARY.md`
</output>
