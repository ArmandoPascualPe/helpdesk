---
phase: 02-features-history
plan: 01
type: execute
wave: 1
depends_on: []  # Depends on Phase 1 completion
files_modified:
  - src/lib/comments.ts
  - src/actions/comments.ts
  - src/lib/attachments.ts
  - src/actions/download-attachment.ts
  - src/app/tickets/[id]/page.tsx
  - src/components/ticket-comment-form.tsx
  - src/components/ticket-comments-list.tsx
  - src/components/ticket-attachment-upload.tsx
  - src/components/ticket-attachments-list.tsx
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, ATT-01, ATT-02, ATT-03]
must_haves:
  truths:
    - "User can see existing comments on a ticket"
    - "User can add a public comment to their ticket"
    - "Agent/supervisor can add an internal comment"
    - "User can upload up to 3 files to a ticket"
    - "User can download any attachment"
  artifacts:
    - path: "src/lib/comments.ts"
      provides: "Comment interface and getCommentsByTicket function"
    - path: "src/actions/comments.ts"
      provides: "addComment Server Action"
    - path: "src/lib/attachments.ts"
      provides: "Attachment validation and getAttachmentsByTicket"
    - path: "src/actions/download-attachment.ts"
      provides: "Authenticated file download"
    - path: "src/components/ticket-comment-form.tsx"
      provides: "Comment form with internal flag"
    - path: "src/components/ticket-comments-list.tsx"
      provides: "Chronological comment display"
    - path: "src/components/ticket-attachment-upload.tsx"
      provides: "Validated file upload (max 3, 5MB, png/jpeg/pdf/zip)"
    - path: "src/components/ticket-attachments-list.tsx"
      provides: "Attachment download UI"
  key_links:
    - from: "src/components/ticket-comment-form.tsx"
      to: "src/actions/comments.ts"
      via: "useServer addComment action"
      pattern: "addComment.*action"
    - from: "src/components/ticket-comments-list.tsx"
      to: "src/lib/comments.ts"
      via: "getCommentsByTicket function"
      pattern: "getCommentsByTicket"
    - from: "src/components/ticket-attachment-upload.tsx"
      to: "src/lib/attachments.ts"
      via: "validateAttachment function"
      pattern: "validateAttachment"
    - from: "src/components/ticket-attachments-list.tsx"
      to: "src/actions/download-attachment.ts"
      via: "downloadAttachment action"
      pattern: "downloadAttachment.*action"
---

<objective>
Implement Comments and Attachments systems for ticket detail page
</objective>

<context>
@.planning/phases/02-features-history/02-CONTEXT.md
@.planning/phases/02-features-history/02-RESEARCH.md
@src/lib/pocketbase.ts
@src/lib/auth.ts
@src/lib/validations.ts

**Locked Decisions to implement exactly:**
- D-01: Public comments visible to all users
- D-02: Internal comments (is_internal=true) visible only to agents/supervisors
- D-03: Chronological order (created asc)
- D-04: Fields: content, is_internal, ticket_id, user_id, created, updated
- D-05: Max 3 files per ticket
- D-06: Max 5MB each
- D-07: Types: png, jpeg, pdf, zip
- D-08: Storage in PocketBase (file field)
- D-09: Download with authentication (Server Action)
</context>

<execution_context>
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Comments library and Server Actions</name>
  <files>src/lib/comments.ts, src/actions/comments.ts</files>
  <read_first>
    - src/lib/auth.ts - for User type and currentUser()
    - src/lib/validations.ts - for Zod patterns
  </read_first>
  <action>
    **Create `src/lib/comments.ts`:**
    - Export interface Comment with fields: id, ticket_id, user_id, content, is_internal, created, updated
    - Export commentSchema using Zod: content (string, min 1), is_internal (boolean), ticket_id (string)
    - Export async function getCommentsByTicket(ticketId, includeInternal?) that queries `comentarios` collection sorted by created asc
    - Internal comments filtered by checking currentUser().rol !== 'cliente' before returning

    **Create `src/actions/comments.ts`:**
    - Server Action `addComment(data: {ticket_id, content, is_internal})`
    - Validates using commentSchema
    - Uses currentUser() from auth.ts to get user_id
    - Creates record in `comentarios` collection
    - Returns created comment
  </action>
  <verify>
    <automated>grep -r "export.*Comment" src/lib/comments.ts && grep -r "addComment" src/actions/comments.ts</automated>
  </verify>
  <done>
    - Comments library exports Comment interface and getCommentsByTicket function
    - Server Action addComment creates comments in PocketBase
    - Internal comments filtered from clients (D-02)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Attachments library and download Server Action</name>
  <files>src/lib/attachments.ts, src/actions/download-attachment.ts</files>
  <read_first>
    - src/lib/pocketbase.ts - for pb instance
  </read_first>
  <action>
    **Create `src/lib/attachments.ts`:**
    - Export interface TicketAttachment with: id, ticket_id, filename, file_size, content_type, created
    - Export const ALLOWED_TYPES = ['png', 'jpeg', 'pdf', 'zip']
    - Export const MAX_FILE_SIZE = 5 * 1024 * 1024 // 5MB
    - Export const MAX_FILES_PER_TICKET = 3
    - Export function validateAttachment(file: File): {valid: boolean, error?: string}
      - Check file extension against ALLOWED_TYPES
      - Check file.size <= MAX_FILE_SIZE
    - Export function getAttachmentsByTicket(ticketId): fetches from tickets collection where ticket_id matches

    **Create `src/actions/download-attachment.ts`:**
    - Server Action `downloadAttachment(ticketId, filename)`
    - Uses pb.collection('tickets').getOne(ticketId) to verify access
    - Checks currentUser() authentication
    - Returns file using pb.files.getUrl() with authentication
    - Sets appropriate headers: Content-Disposition: attachment; filename=...
  </action>
  <verify>
    <automated>grep -r "validateAttachment" src/lib/attachments.ts && grep -r "downloadAttachment" src/actions/download-attachment.ts</automated>
  </verify>
  <done>
    - Attachments library with validation (D-05, D-06, D-07)
    - Download Server Action with auth check (D-09)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Comment and Attachment UI components</name>
  <files>src/components/ticket-comment-form.tsx, src/components/ticket-comments-list.tsx, src/components/ticket-attachment-upload.tsx, src/components/ticket-attachments-list.tsx</files>
  <read_first>
    - src/app/tickets/[id]/page.tsx - to understand ticket detail structure
    - src/lib/auth.ts - for role checking
  </read_first>
  <action>
    **Create `src/components/ticket-comment-form.tsx`:**
    - Uses React Hook Form + Zod validation
    - Fields: content (textarea), is_internal (checkbox, only visible for agente/supervisor roles)
    - Submit calls addComment Server Action
    - After submit, clears form and triggers refresh

    **Create `src/components/ticket-comments-list.tsx`:**
    - Props: ticketId
    - Fetches comments using getCommentsByTicket
    - Filters internal comments for cliente role (D-02)
    - Renders each comment: user info, content, timestamp, "Internal" badge if is_internal
    - Sorted by created asc (D-03)

    **Create `src/components/ticket-attachment-upload.tsx`:**
    - Props: ticketId
    - File input accepting .png,.jpeg,.pdf,.zip
    - Validates before upload: max 3 files (D-05), max 5MB (D-06), allowed types (D-07)
    - Upload to tickets collection via pb.collection('tickets').update(ticketId, formData)
    - Shows progress indicator
    - Displays error messages for validation failures

    **Create `src/components/ticket-attachments-list.tsx`:**
    - Props: ticketId, attachments[]
    - Each attachment shows filename, size, download button
    - Download button calls downloadAttachment Server Action
    - Max 3 files indicator
  </action>
  <verify>
    <automated>ls src/components/ticket-comment-form.tsx src/components/ticket-comments-list.tsx src/components/ticket-attachment-upload.tsx src/components/ticket-attachments-list.tsx</automated>
  </verify>
  <done>
    - Comment form with internal flag for agents/supervisors
    - Comments list showing chronological order
    - Attachment upload with validation
    - Attachment download functionality
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→addComment | Untrusted content crosses here |
| client→upload | Untrusted file data crosses here |
| client→download | Untrusted ticket/filename params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S/I | addComment | mitigate | Validate content with Zod, sanitize HTML if rich text |
| T-02-02 | T | upload | mitigate | Validate file type server-side (ALLOWED_TYPES), reject if invalid |
| T-02-03 | T | upload | mitigate | Validate file size server-side (MAX_FILE_SIZE), reject if >5MB |
| T-02-04 | I | internal comments | mitigate | Filter is_internal=true at API level for cliente role |
| T-02-05 | S | download | mitigate | Verify user is authenticated, verify ticket access before serving file |
</threat_model>


[Overall phase checks]
- Comments CRUD works with role-based filtering
- Attachments upload respects 3-file limit, 5MB limit, allowed types
- Attachments download requires authentication
</verification>

<success_criteria>
- COMM-01: User can add public comments to a ticket
- COMM-02: Agent/supervisor can add internal comments (is_internal flag, RBAC filter)
- COMM-03: Comments display in chronological order (created asc)
- ATT-01: User can attach up to 3 files per ticket
- ATT-02: Files max 5MB, types: png, jpeg, pdf, zip
- ATT-03: User can download attachments with authentication
</success_criteria>

<output>
After completion, create `.planning/phases/02-features-history/02-01-SUMMARY.md`
</output>
