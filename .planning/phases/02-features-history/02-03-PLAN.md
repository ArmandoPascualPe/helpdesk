---
phase: 02-features-history
plan: 03
type: execute
wave: 1
depends_on: []  # Depends on Phase 1 completion
files_modified:
  - src/lib/history.ts
  - src/actions/record-history.ts
  - src/components/ticket-history-list.tsx
  - src/app/tickets/[id]/page.tsx
  - pb_schema.json (comments, attachments, history collections)
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "User can see ticket change history"
    - "Each history entry shows what changed, who, and when"
  artifacts:
    - path: "pb_schema.json"
      provides: "comentarios, historial, adjuntos schema definitions"
    - path: "src/lib/history.ts"
      provides: "HistoryEntry interface, getHistoryByTicket, recordHistory"
    - path: "src/actions/record-history.ts"
      provides: "recordTicketHistory Server Action"
    - path: "src/components/ticket-history-list.tsx"
      provides: "History display with change_type, old/new values, user, timestamp"
  key_links:
    - from: "src/components/ticket-history-list.tsx"
      to: "src/lib/history.ts"
      via: "getHistoryByTicket function"
      pattern: "getHistoryByTicket"
    - from: "ticket update action"
      to: "src/actions/record-history.ts"
      via: "recordTicketHistory on state/assignment/priority change"
      pattern: "recordTicketHistory"
---

<objective>
Implement Audit History system for ticket changes
</objective>

<context>
@.planning/phases/02-features-history/02-CONTEXT.md
@.planning/phases/02-features-history/02-RESEARCH.md
@src/lib/pocketbase.ts
@src/lib/auth.ts

**Locked Decisions to implement exactly:**
- D-14: Table history with fields: ticket_id, user_id, change_type, old_value, new_value, created
- D-15: change_types: STATE_CHANGE, ASSIGNMENT_CHANGE, PRIORITY_CHANGE
- D-16: Auto-registration via PocketBase hooks
</context>

<execution_context>
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/armando.pascual/Documents/helpdesktop/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update PocketBase schema and create History library</name>
  <files>pb_schema.json, src/lib/history.ts</files>
  <read_first>
    - pb_schema.json - current schema (if exists, create if not)
    - src/lib/pocketbase.ts - for pb instance
  </read_first>
  <action>
    **Update `pb_schema.json` (or create if first schema file):**
    - Add `comentarios` collection:
      ```
      {
        "name": "comentarios",
        "type": "base",
        "system": false,
        "schema": [
          {"system": false, "name": "ticket_id", "type": "relation", "required": true, "unique": false, "options": {"collectionId": "tickets", "cascadeDelete": true}},
          {"system": false, "name": "user_id", "type": "relation", "required": true, "unique": false, "options": {"collectionId": "usuarios", "cascadeDelete": false}},
          {"system": false, "name": "content", "type": "text", "required": true, "unique": false},
          {"system": false, "name": "is_internal", "type": "bool", "required": true, "unique": false, "options": {"default": false}}
        ]
      }
      ```
    - Add `historial` collection:
      ```
      {
        "name": "historial",
        "type": "base",
        "system": false,
        "schema": [
          {"system": false, "name": "ticket_id", "type": "relation", "required": true, "unique": false, "options": {"collectionId": "tickets", "cascadeDelete": true}},
          {"system": false, "name": "user_id", "type": "relation", "required": true, "unique": false, "options": {"collectionId": "usuarios", "cascadeDelete": false}},
          {"system": false, "name": "change_type", "type": "select", "required": true, "unique": false, "options": {"values": ["STATE_CHANGE", "ASSIGNMENT_CHANGE", "PRIORITY_CHANGE"]}},
          {"system": false, "name": "old_value", "type": "text", "required": false, "unique": false},
          {"system": false, "name": "new_value", "type": "text", "required": false, "unique": false}
        ]
      }
      ```
    - Add `adjuntos` field to tickets collection: {"name": "adjuntos", "type": "file", "maxCount": 3, "maxSize": 5242880, "mimeTypes": ["image/png", "image/jpeg", "application/pdf", "application/zip"]}

    **Create `src/lib/history.ts`:**
    - Export type ChangeType = 'STATE_CHANGE' | 'ASSIGNMENT_CHANGE' | 'PRIORITY_CHANGE'
    - Export interface HistoryEntry: id, ticket_id, user_id, change_type, old_value, new_value, created
    - Export function getHistoryByTicket(ticketId): fetches from historial collection sorted by created desc
    - Export function recordHistory(data: {ticket_id, change_type, old_value, new_value}): creates entry in historial
  </action>
  <verify>
    <automated>grep -r "historial" pb_schema.json && grep -r "HistoryEntry" src/lib/history.ts</automated>
  </verify>
  <done>
    - PocketBase schema updated with comentarios, historial collections
    - History library with getHistoryByTicket and recordHistory functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate History recording into ticket updates</name>
  <files>src/actions/record-history.ts, src/actions/update-ticket.ts (existing or new)</files>
  <read_first>
    - src/lib/history.ts - for recordHistory function
    - src/lib/auth.ts - for currentUser()
  </read_first>
  <action>
    **Create `src/actions/record-history.ts`:**
    - Server Action `recordTicketHistory(data: {ticket_id, change_type, old_value, new_value})`
    - Gets currentUser() for user_id
    - Calls recordHistory from lib/history.ts
    - Returns created history entry

    **Modify ticket update action (create if doesn't exist):**
    - When ticket state changes: recordHistory with change_type='STATE_CHANGE'
    - When ticket assignment changes: recordHistory with change_type='ASSIGNMENT_CHANGE'
    - When ticket priority changes: recordHistory with change_type='PRIORITY_CHANGE'
    - Only record if old_value !== new_value (avoid duplicates)
    - This implements D-16: auto-registration
  </action>
  <verify>
    <automated>grep -r "recordHistory" src/actions/record-history.ts && grep -r "STATE_CHANGE" src/actions/record-history.ts</automated>
  </verify>
  <done>
    - History is automatically recorded on ticket updates
    - All three change types tracked: state, assignment, priority
  </done>
</task>

<task type="auto">
  <name>Task 3: Create History UI component and integrate into ticket detail</name>
  <files>src/components/ticket-history-list.tsx, src/app/tickets/[id]/page.tsx</files>
  <read_first>
    - src/lib/history.ts - for getHistoryByTicket
    - src/app/tickets/[id]/page.tsx - current ticket detail page
  </read_first>
  <action>
    **Create `src/components/ticket-history-list.tsx`:**
    - Props: ticketId
    - Fetches history using getHistoryByTicket
    - Renders each entry: change_type badge, old_value → new_value, user who made change, timestamp
    - Format change_type display:
      - STATE_CHANGE: "Estado cambiado"
      - ASSIGNMENT_CHANGE: "Asignación cambiada"
      - PRIORITY_CHANGE: "Prioridad cambiada"
    - Shows "No hay historial" when empty
    - Visible to all authenticated users

    **Update `src/app/tickets/[id]/page.tsx`:**
    - Import ticket-history-list component
    - Add history section to ticket detail page
    - Fetch and display history entries
  </action>
  <verify>
    <automated>ls src/components/ticket-history-list.tsx && grep -r "ticket-history-list" src/app/tickets/[id]/page.tsx</automated>
  </verify>
  <done>
    - History list component shows all ticket changes
    - Each entry shows what changed, who, and when
    - Integrated into ticket detail page
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→recordHistory | Untrusted change data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | I | history records | mitigate | Only authenticated users can record history, verify ticket access |
| T-02-09 | T | history recording | mitigate | Only record if old_value !== new_value (prevent spam) |
</threat_model>


[Overall phase checks]
- State changes recorded with timestamp and user
- Assignment changes recorded with timestamp and user
- Priority changes recorded with timestamp and user
- History visible in ticket detail page
</verification>

<success_criteria>
- HIST-01: System records state changes with timestamp and user
- HIST-02: System records assignment changes with timestamp and user
- HIST-03: System records priority changes with timestamp and user
</success_criteria>

<output>
After completion, create `.planning/phases/02-features-history/02-03-SUMMARY.md`
</output>
