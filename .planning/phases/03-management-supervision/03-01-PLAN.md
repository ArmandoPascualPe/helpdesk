---
phase: 03-management-supervision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/departments.ts
  - src/actions/departments.ts
  - src/components/department-list.tsx
  - src/app/dashboard/departments/page.tsx
autonomous: true
requirements:
  - DEPT-01
  - DEPT-02
  - DEPT-03
  - DEPT-04

must_haves:
  truths:
    - "Supervisor puede crear nuevos departamentos con nombre y descripción"
    - "Supervisor puede editar detalles de departamentos existentes"
    - "Supervisor puede desactivar departamentos (solo si no hay tickets abiertos)"
    - "Supervisor puede asignar agentes a departamentos"
  artifacts:
    - path: "src/lib/departments.ts"
      provides: "Department CRUD functions"
      exports: ["getDepartments", "createDepartment", "updateDepartment", "deactivateDepartment", "assignAgentToDepartment", "hasOpenTickets"]
    - path: "src/actions/departments.ts"
      provides: "Server Actions for department management"
      exports: ["createDepartmentAction", "updateDepartmentAction", "deactivateDepartmentAction", "assignAgentAction"]
    - path: "src/components/department-list.tsx"
      provides: "Department management UI (list + actions)"
      features: ["create form", "edit form", "deactivate with validation", "agent assignment"]
    - path: "src/app/dashboard/departments/page.tsx"
      provides: "Dedicated page for department management (per D-01)"
  key_links:
    - from: "department-list.tsx"
      to: "/actions/departments.ts"
      via: "Server Actions"
      pattern: "createDepartmentAction|updateDepartmentAction|deactivateDepartmentAction"
    - from: "departments.ts"
      to: "pocketbase.ts"
      via: "PocketBase client"
      pattern: "pb.collection\\('departamentos'\\)"
---

<objective>
Gestión completa de departamentos: crear, editar, desactivar con validación, y asignar agentes. Implementa decisiones D-01 (página dedicada), D-02 (campos mínimos), D-03 (validación de desactivación), y D-04 (asignación de agentes).
</objective>

<context>
@.planning/REQUIREMENTS.md (DEPT-01, DEPT-02, DEPT-03, DEPT-04)
@.planning/phases/03-management-supervision/03-CONTEXT.md (User decisions)
@src/lib/auth.ts (User interface con campo departamento)
@src/lib/pocketbase.ts (PocketBase client)

**Colección departamentos ya existe** en PocketBase con campos: nombre, descripcion, email, activo
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create department lib functions</name>
  <files>src/lib/departments.ts</files>
  <action>
Create src/lib/departments.ts with:
- getDepartments(): Get all departments (including inactive for management)
- createDepartment(name, description): Create new department
- updateDepartment(id, name, description): Update department details
- deactivateDepartment(id): Check if has open tickets, then deactivate (per D-03)
- hasOpenTickets(departmentId): Check if department has open tickets
- getAgentsByDepartment(departmentId): Get agents assigned to department
- assignAgentToDepartment(agentId, departmentId): Assign agent to department

Use PocketBase client from src/lib/pocketbase.ts. Return proper error handling.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/lib/departments.ts</automated>
  </verify>
  <done>Department CRUD functions exported, deactivate validates open tickets</done>
</task>

<task type="auto">
  <name>Task 2: Create Server Actions for departments</name>
  <files>src/actions/departments.ts</files>
  <action>
Create Server Actions for department management:
- createDepartmentAction(formData): Validate, call lib, return result
- updateDepartmentAction(id, formData): Validate, call lib, return result
- deactivateDepartmentAction(id): Call lib which validates, return result
- assignAgentAction(agentId, departmentId): Validate user is supervisor, assign

Use zod for validation (name: 3-50 chars, description: optional max 500 chars).
Check current user is supervisor before allowing mutations.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/actions/departments.ts</automated>
  </verify>
  <done>Server Actions with validation, authorization checks</done>
</task>

<task type="auto">
  <name>Task 3: Create department management UI</name>
  <files>src/components/department-list.tsx, src/app/dashboard/departments/page.tsx</files>
  <action>
Create department list component with:
- List view of all departments with name, description, status
- Create form (name + description per D-02)
- Edit inline or modal for existing departments
- Deactivate button with confirmation (validates D-03)
- Agent assignment section showing agents in department
- Add ability to assign agents from a dropdown of available agents

Per D-01: Use dedicated page, not modal. Use shadcn/ui components.
Include proper loading states and error handling.
  </action>
  <verify>
    <automated>Build check: npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Full department management page with create, edit, deactivate, assign agents</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Actions | Untrusted form data crosses here |
| Server Actions → PocketBase | Internal business logic validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | department actions | mitigate | Validate name length (3-50) and sanitize description |
| T-03-02 | T | deactivate department | mitigate | Check open tickets before allowing deactivate (enforced in lib) |
| T-03-03 | I | assign agent | mitigate | Verify user has supervisor role before assignment |
</threat_model>

<verification>
- Department CRUD works with PocketBase collection 'departamentos'
- Deactivation blocked when open tickets exist (per D-03)
- Only supervisors can manage departments
</verification>

<success_criteria>
Supervisor puede crear, editar, desactivar departamentos y asignar agentes. Desactivación validada contra tickets abiertos.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-supervision/03-01-SUMMARY.md`
</output>