---
phase: 03-management-supervision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-charts.tsx
  - src/components/agent-workload.tsx
  - src/components/overdue-tickets.tsx
  - src/app/dashboard/supervisor/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Supervisor puede ver dashboard con conteo de tickets por estado"
    - "Supervisor puede ver dashboard con conteo de tickets por prioridad"
    - "Supervisor puede ver dashboard con conteo de tickets por departamento"
    - "Supervisor puede ver carga de trabajo de agentes"
    - "Supervisor puede ver tiempo promedio de resolución"
    - "Supervisor puede ver lista de tickets abiertos más de 7 días"
  artifacts:
    - path: "src/components/dashboard-charts.tsx"
      provides: "Recharts components for status/priority/department breakdowns"
      exports: ["TicketStatusChart", "TicketPriorityChart", "TicketDepartmentChart"]
    - path: "src/components/agent-workload.tsx"
      provides: "Agent workload metrics with open + resolved tickets (per D-07)"
      features: ["sort by total load highest to lowest (D-08)"]
    - path: "src/components/overdue-tickets.tsx"
      provides: "List of tickets open > threshold days"
      features: ["configurable threshold per supervisor (D-09)", "visual highlight (D-10)"]
    - path: "src/app/dashboard/supervisor/page.tsx"
      provides: "Supervisor dashboard (per D-05: location in supervisor menu)"
  key_links:
    - from: "dashboard-charts.tsx"
      to: "tickets collection"
      via: "PocketBase queries"
      pattern: "pb.collection\\('tickets'\\).getList"
    - from: "agent-workload.tsx"
      to: "usuarios collection"
      via: "PocketBase queries"
      pattern: "filter: agente"
---

<objective>
Dashboard completo con métricas operacionales: gráficos de status/prioridad/departamento, workload de agentes, tiempo de resolución promedio, y lista de tickets vencidos. Per D-04: gráficos + tablas combinadas. Per D-05: ubicado en menú supervisor. Per D-06: load-only.
</objective>

<context>
@.planning/REQUIREMENTS.md (DASH-01 a DASH-06)
@.planning/phases/03-management-supervision/03-CONTEXT.md (User decisions)
@src/components/ticket-history-list.tsx (Pattern reference for lists)

**Recharts already available** in project (Phase 2 SUMMARY confirmed)

**Note:** resolved_at field needed for DASH-05 - Plan 03 will handle schema
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard charts component</name>
  <files>src/components/dashboard-charts.tsx</files>
  <action>
Create dashboard charts using Recharts:
- TicketStatusChart: Pie/Donut chart showing tickets by status (abierto, en_proceso, cerrado)
- TicketPriorityChart: Bar chart showing tickets by priority (baja, media, alta, critica)
- TicketDepartmentChart: Bar chart showing tickets by department

Use data from PocketBase tickets collection. Per D-04: combine charts + tables.
Use colors matching shadcn/ui design system.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/components/dashboard-charts.tsx</automated>
  </verify>
  <done>Three chart components for status, priority, department breakdowns</done>
</task>

<task type="auto">
  <name>Task 2: Create agent workload component</name>
  <files>src/components/agent-workload.tsx</files>
  <action>
Create agent workload display:
- Query usuarios collection for agents (rol = 'agente')
- For each agent: count open tickets (status != 'cerrado') AND resolved tickets (status = 'cerrado')
- Display in table: Agent name, Open tickets, Resolved tickets, Total load
- Sort by total load descending (per D-08: mayor a menor)

Per D-07: Show open + resolved tickets for complete breakdown.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/components/agent-workload.tsx</automated>
  </verify>
  <done>Agent workload table sorted by total tickets (highest to lowest)</done>
</task>

<task type="auto">
<name>Task 3: Create overdue tickets component</name>
<files>src/components/overdue-tickets.tsx</files>
<action>
Create overdue tickets list:
- Accept configurable thresholdDays prop (per D-09: configurable by supervisor)
- Query tickets where created < thresholdDays ago AND status != 'cerrado'
- Display in list/table with: ticket number, title, created date, days open
- Apply distinctive visual highlight for overdue (per D-10): red/orange background or border

Make threshold configurable via prop, default 7 days.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/components/overdue-tickets.tsx</automated>
  </verify>
  <done>Overdue tickets list with configurable threshold and visual highlight</done>
</task>

<task type="auto">
  <name>Task 4: Create supervisor dashboard page</name>
  <files>src/app/dashboard/supervisor/page.tsx</files>
  <action>
Create supervisor dashboard page:
- Integrate all charts (status, priority, department)
- Include agent workload component
- Include overdue tickets component
- Per D-05: located at /dashboard/supervisor (in supervisor menu)
- Per D-06: load-only (use Server Component with initial fetch, no real-time)

Layout: Charts in top section, tables below. Use responsive grid.
  </action>
  <verify>
    <automated>Build check: npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Complete supervisor dashboard at /dashboard/supervisor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard → PocketBase | Read-only queries for metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | dashboard queries | accept | Read-only queries, no injection risk |
| T-03-05 | S | overdue threshold | mitigate | Validate threshold is positive number |
</threat_model>

<verification>
- Dashboard loads all 6 metrics (DASH-01 to DASH-06)
- Charts display correct data from PocketBase
- Per D-05: visible in supervisor menu navigation
</verification>

<success_criteria>
Supervisor puede ver dashboard completo con métricas, workload de agentes, y tickets vencidos.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-supervision/03-02-SUMMARY.md`
</output>