---
phase: 03-management-supervision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ticket-metrics.ts
  - src/components/avg-resolution-time.tsx
autonomous: true
requirements:
  - DASH-05

must_haves:
  truths:
    - "Supervisor puede ver tiempo promedio de resolución de tickets"
  artifacts:
    - path: "src/lib/ticket-metrics.ts"
      provides: "Metrics queries including resolved_at handling"
      exports: ["getAverageResolutionTime", "getTicketsByStatus", "getTicketsByPriority", "getTicketsByDepartment"]
    - path: "src/components/avg-resolution-time.tsx"
      provides: "Average resolution time display component"
  key_links:
    - from: "avg-resolution-time.tsx"
      to: "tickets collection"
      via: "PocketBase with resolved_at field"
      pattern: "DATEDIFF(created, resolved)"
---

<objective>
Agregar campo resolved_at a tickets collection y crear componente de métricas para tiempo de resolución promedio. El campo resolved_at es necesario para DASH-05 y actualmente no existe en la colección.
</objective>

<context>
@.planning/REQUIREMENTS.md (DASH-05: tiempo promedio de resolución)
@.planning/phases/03-management-supervision/03-CONTEXT.md (resolved_at does not exist)

**Schema Change Required:**
- Agregar campo 'resolved_at' (date) a colección 'tickets' en PocketBase
- Actualizar ticket actions para setear resolved_at cuando status = 'cerrado'
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resolved_at schema to PocketBase</name>
  <files>src/lib/ticket-metrics.ts</files>
  <action>
**IMPORTANT:** This task requires manual PocketBase schema update.

1. Acceder a PocketBase Admin UI (http://127.0.0.1:8090/_/)
2. Ir a colección 'tickets'
3. Agregar campo 'resolved_at' tipo 'date'
4. Guardar cambios

After schema update, create src/lib/ticket-metrics.ts:
- getAverageResolutionTime(): Calculate average days between created and resolved_at for closed tickets
- getTicketsByStatus(): Group tickets by status
- getTicketsByPriority(): Group tickets by priority
- getTicketsByDepartment(): Group tickets by department

Note: This uses DATEDIFF function from PocketBase or calculate client-side.
  </action>
  <verify>
    <automated>TypeScript compilation after PB schema update: npx tsc --noEmit src/lib/ticket-metrics.ts</automated>
  </verify>
  <done>PocketBase schema updated, metrics lib created</done>
</task>

<task type="auto">
  <name>Task 2: Update ticket actions to set resolved_at</name>
  <files>src/app/dashboard/tickets/[id]/actions.ts</files>
  <action>
Update ticket state change action in src/app/dashboard/tickets/[id]/actions.ts:
- When status changes to 'cerrado', set resolved_at = new Date().toISOString()
- When status changes FROM 'cerrado' to anything else, clear resolved_at (reopened)

This ensures DASH-05 can calculate average resolution time accurately.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/app/dashboard/tickets/[id]/actions.ts</automated>
  </verify>
  <done>Ticket actions update resolved_at on close/reopen</done>
</task>

<task type="auto">
  <name>Task 3: Create average resolution time component</name>
  <files>src/components/avg-resolution-time.tsx</files>
  <action>
Create avg-resolution-time.tsx component:
- Fetch closed tickets with resolved_at set
- Calculate average days from created to resolved
- Display as "X días promedio" or similar
- Handle case with no resolved tickets (show "Sin datos" or 0)

Use ticket-metrics.ts for the calculation.
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit src/components/avg-resolution-time.tsx</automated>
  </verify>
  <done>Average resolution time component created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual: PocketBase Admin | Requires human to update schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | I | resolved_at update | mitigate | Validate resolved_at is valid date or null |
</threat_model>

<verification>
- resolved_at field added to tickets collection
- Ticket close action sets resolved_at
- Average resolution time calculated correctly
</verification>

<success_criteria>
DASH-05 implementado: supervisor puede ver tiempo promedio de resolución de tickets.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-supervision/03-03-SUMMARY.md`
</output>